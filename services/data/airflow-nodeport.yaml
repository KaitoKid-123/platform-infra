apiVersion: v1
kind: Service
metadata:
  name: airflow-webserver-external
  namespace: platform-data
spec:
  type: NodePort
  selector:
    component: webserver
    release: airflow
  ports:
    - name: airflow-ui
      port: 8080
      targetPort: 8080
      nodePort: 30080
