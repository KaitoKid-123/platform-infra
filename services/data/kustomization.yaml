apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - github-dags-token.yaml
  - airflow-nodeport.yaml
helmCharts:
  - name: airflow
    repo: https://airflow.apache.org
    version: 1.11.0
    releaseName: airflow
    namespace: platform-data
    valuesFile: airflow/values.yaml
  # [DISABLED - cluster quá nhỏ] OpenMetadata: cần 1+ CPU, 3+ GB RAM (ES+MySQL)
  # - name: openmetadata
  #   repo: https://helm.open-metadata.org
  #   version: 1.3.0
  #   releaseName: openmetadata
  #   namespace: platform-data
  #   valuesFile: openmetadata/values.yaml
  # - name: openmetadata-dependencies
  #   repo: https://helm.open-metadata.org
  #   version: 1.3.0
  #   releaseName: openmetadata-dependencies
  #   namespace: platform-data
  #   valuesFile: openmetadata/deps-values.yaml