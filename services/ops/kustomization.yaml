# Gitea resources removed - migrated to GitHub + GHCR
# All Gitea runner resources are now pruned by ArgoCD
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - placeholder-configmap.yaml
