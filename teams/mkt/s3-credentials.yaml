# Template: MinIO credentials for new teams
# Same credentials used across all teams in dev environment
apiVersion: v1
kind: Secret
metadata:
  name: team-mkt-s3-creds
  namespace: team-mkt
type: Opaque
stringData:
  access_key: "minioadmin"
  secret_key: "MinIO@Admin2024!"
