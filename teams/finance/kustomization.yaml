# Đây là ví dụ cho team finance — được sinh ra bởi onboard-team.sh
# Tất cả file khác trong teams/finance/ giống _template nhưng TEAM_NAME=finance
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespace.yaml
  - resource-quota.yaml
  - network-policy.yaml
  - rbac/service-account.yaml
  - rbac/role-binding.yaml
  - rbac/airflow-sparkapplication-role.yaml