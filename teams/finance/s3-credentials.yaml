# MinIO credentials cho team-finance namespace
# Plain Secret cho dev cluster (khong co sealed-secrets controller)
apiVersion: v1
kind: Secret
metadata:
  name: team-finance-s3-creds
  namespace: team-finance
type: Opaque
stringData:
  access_key: "minioadmin"
  secret_key: "MinIO@Admin2024!"
