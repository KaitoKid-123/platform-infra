# Plain Secret cho dev cluster (khong co sealed-secrets controller)
# Chua MinIO credentials cho team-sale namespace
# Doi sang SealedSecret khi co sealed-secrets controller
apiVersion: v1
kind: Secret
metadata:
  name: team-sale-s3-creds
  namespace: team-sale
type: Opaque
stringData:
  access_key: "minioadmin"
  secret_key: "MinIO@Admin2024!"
